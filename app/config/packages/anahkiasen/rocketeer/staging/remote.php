<?php global $app;
return array(

	// Folders
	////////////////////////////////////////////////////////////////////

	// The root directory where your applications will be deployed
	'root_directory'   => $app['config']->get('remote.connections.staging.root'),

);
